"""Shared example JSON basenames for Plan 08 compiler syrupy goldens (IR + emit).

Keep **one** list so IR and emit snapshots stay aligned on the same fixtures."""

COMPILER_SNAPSHOT_EXAMPLE_FILES = [
    "funds_flow_demo.json",
    "marketplace_demo.json",
    "stablecoin_ramp.json",
    "staged_demo.json",
    "lending_platform.json",
]
