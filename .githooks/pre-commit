#!/bin/sh
# Refuse commits that stage anything under top-level plan/
set -eu
if git diff --cached --name-only --diff-filter=ACMR | grep -qE '^plan(/|$)'; then
  echo "pre-commit: do not commit files under plan/ (git reset -- <paths> to unstage)" >&2
  exit 1
fi
